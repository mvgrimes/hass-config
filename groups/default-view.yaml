default_view:
  view: yes
  name: Default
  entities:
    - group.weather_simple
    - group.switch_group
    - group.mode_group
    - group.status_panel
    - group.finance

mode_group:
  name: Modes
  control: hidden
  entities:
    - group.home
    - group.vaction

switch_group:
  name: Switches
  control: hidden
  entities:
    - group.exterior_lights
    - group.basement_lights
    - group.locks
    - group.family_room
