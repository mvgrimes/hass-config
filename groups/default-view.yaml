default_view:
  view: yes
  name: Default
  entities:
    - group.notices
    - group.weather_simple
    - group.switch_group
    - group.modes
    - group.status_panel
    - group.finance

switch_group:
  name: Switches
  control: hidden
  entities:
    - group.exterior_lights
    - group.basement_lights
    - group.locks
    - group.family_room

notices:
  name: Notices
  control: hidden
  entities:
    - updater.updater
