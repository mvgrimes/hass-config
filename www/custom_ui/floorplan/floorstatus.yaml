# This is the main Floorplan configuration page.  To define the pages.
## This one is called Floorclock since this is for my Clock application.
## log_level: which can be set to one these: error, warning,  info or debug

# date_format: MMM-DD-YYYY
# log_level: debug

fully_kiosk:
  - name: Status Panel
    address: 18:74:2E:00:5F:E5
    motion_sensor: binary_sensor.status_motion
    plugged_sensor: binary_sensor.status_plugged
    screensaver_light: light.status_screensaver
    # media_player: media_player.status_panel

pages:
  - /local/custom_ui/floorplan/status.yaml
